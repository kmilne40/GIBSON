# john Simulator Enrichment Implementation

`john` remains a bounded Gibson simulator. It reads Gibson hash datasets and Gibson wordlists only, rejects host paths, performs dictionary matching against simulated RACF legacy-DES hashes and supports `--show`, `--status` and `--json`. It emits SMF30/92 evidence plus Master Console alerts.
