# RACFDS Hash Test Report

Covered `RACFDB SEED LEGACY`, `RACFDB VERIFY HASHES`, visible legacy-DES records in `SYS1.RACFDS(DATABASE)`, `racf2john --summary`, hash extraction, `john` cracking and Master Console evidence.
