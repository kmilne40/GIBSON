# SMF Recording Mode Implementation

Structured SMF records are now mirrored to the active MAN dataset in RECORDING(DATASET) mode. Commands include D SMF, D SMF,DS, SMF SWITCH, SMF DUMP MAN(...), SMF RECORDING(LOGSTREAM), and SMF DUMP LOGSTREAM(...).
