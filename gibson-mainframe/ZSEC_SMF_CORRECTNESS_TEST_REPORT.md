# ZSEC / SMF correctness test report

New tests validate that `ZSEC EVENTS` differs from `ZSEC RARE`, that offline hash views treat `LEGACY-DES-SIM` as simulator-crackable, that SMF records populate after IND$FILE events, and that period summaries are distinct.
