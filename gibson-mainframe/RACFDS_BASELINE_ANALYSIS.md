# RACFDS Baseline Analysis

Gibson previously used GACF.DB as the active authentication source and contained placeholder SYS1.RACFDS/SYS1.RACFDS.BACKUP datasets. This package makes SYS1.RACFDS a structured simulated RACF database artefact while preserving GACF.DB as the authentication source of truth.
