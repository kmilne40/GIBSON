# Livingston Home Network Override

`192.168.0.0/24` is treated as a configured local home/lab network and maps to Livingston, West Lothian, Scotland, GB using city-level approximate coordinates. The source is `local-override`, classification is `private_home_network`, and public providers are not called for these addresses.
