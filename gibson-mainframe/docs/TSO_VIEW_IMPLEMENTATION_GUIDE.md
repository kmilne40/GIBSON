# TSO VIEW Implementation Guide

TSO already contains a `VIEW` command path that calls the dataset viewer. No mutating behaviour was added. The uploaded VIEW-specific material was not separately identifiable beyond existing REXX/TSO assets, so this build preserves and tests the current safe TSO `VIEW dataset` behaviour.
