# OMVS OSINT Tools Guide

Commands: `subfinder`, `dig`, `whois`, `shodan`, `ezrecon`, `geoloc`, `cti-rss`.

The default mode is offline fixture mode. API keys are never hard-coded or displayed. Active scanning still requires HOSTS.TXT authorisation.
