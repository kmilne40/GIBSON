# Terminal key handling test report

New tests in `tests/test_terminal_zsec_indfile_fix_v1.py` validate caret-form PF key recognition, socket-driver sequence maps, panel input normalization, and prevention of visible PF sequence pollution.
