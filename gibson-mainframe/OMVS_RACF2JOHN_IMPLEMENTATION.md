# OMVS racf2john Implementation

`racf2john` extracts only LEGACY-DES records from Gibson simulated RACFDS datasets, writes John-style `$racf$` hash datasets and emits SMF80/30/92 plus Master Console and CTI-style alerts.
