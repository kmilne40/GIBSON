# Known Limitations

- This package does not add Option 5 language-workbench functionality.
- The editor still uses a bounded ANSI/telnet editing model rather than a full 3270 field-mode editor.
- Horizontal scrolling beyond the visible data field remains limited; the fix prevents visual auto-wrap rather than implementing a full ISPF horizontal-scroll subsystem.
